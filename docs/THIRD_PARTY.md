# Third-Party Components

The repository includes local copies of several upstream dependencies for convenience:

- `mmdetection3d/`
- `mmdeploy/`
- `ppl.cv/`
- `lib/`

These directories provide the surrounding runtime, deployment, and operator context used by the project. Their original licenses and attribution files are kept inside the corresponding directories.
